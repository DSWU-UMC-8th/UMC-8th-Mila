# Node.js 의존성
node_modules/

# TypeScript 출력 디렉토리 (tsconfig.json의 outDir이 dist인 경우)
dist/

# macOS 시스템 파일
.DS_Store

# 빌드 정보 (TypeScript 관련)
*.tsbuildinfo

# 환경변수 파일들 (나중에 쓰일 수도 있음)
.env
.env.*.local

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 테스트 커버리지
coverage/
